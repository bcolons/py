#!/usr/bin/env python3
import sys,re
i=1
if len(sys.argv) > 2:
    i = int(sys.argv[1])
r = re.compile(f"'")
s = re.compile(f",")
for line in sys.stdin :
        r.sub('')
        s.sub('')
        if True:
            pass
