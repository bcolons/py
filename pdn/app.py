import sys
def load(file):
    with open(file,'r') as f:
        sbuf=f.read()
    return dpar(sbuf))
def dpar(sbuf):
    '''Tokenize leading spaces and colons, parse into nested dict's '''
    
load(sys.argv[0])
